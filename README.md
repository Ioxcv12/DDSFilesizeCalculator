Calculates DDS/PNG format filesize.
Now an HTML app! For old C++ code look in old commits. 
To use, just download the source code for the HTML app. Should download as a ZIP; simply unpack. Pretty much any device should be capable of extracting ZIP files and this should work on most modern browsers.
However, note that mipmaps are incorrect and are fixed in the HTML version. 
